interface SharedExports {
  // Adicione aqui as propriedades específicas que serão exportadas
  [key: string]: any;
}

// Objeto que será exportado
export const shared: SharedExports = {};

// Não é necessário definir __esModule manualmente em TypeScript
// pois o compilador cuida disso automaticamente 